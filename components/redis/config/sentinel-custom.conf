# ==============================================================================
# Redis Sentinel Custom Configuration
# ==============================================================================
# This file is intended for environment-specific overrides.
# It is included by the base sentinel.conf.
# ==============================================================================

# Monitor the master.
# Format: sentinel monitor <name> <host> <port> <quorum>
#
# We define this here so overlays can adjust the quorum (e.g., 2 for 3 sentinels).
sentinel monitor mymaster redis-0.redis-headless 6379 2